import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Frequency Quantique v2',
  webDir: 'dist'
};

export default config;
